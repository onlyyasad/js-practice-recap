

function averageMaker(arr){
    let total = 0;
    for (number of arr){
        total += number;
    }
    let average = total / arr.length;
    return average;
}

const numbers = [40, 70, 25, 35, 30];
const theAverage = averageMaker(numbers);
console.log(theAverage)